<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For Sherilyn ❤️</title>
    <style>
        /* General Styles */
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #ff7eb3, #ff758c);
            margin: 0;
            padding: 0;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: white;
            text-align: center;
        }

        /* Container Design */
        .container {
            background: rgba(255, 255, 255, 0.2);
            border-radius: 15px;
            padding: 20px 30px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(5px);
            z-index: 10; /* Ensure the content is above animations */
        }

        h1 {
            font-size: 2.5rem;
            margin: 0 0 10px 0;
        }

        p {
            font-size: 1.2rem;
            margin: 10px 0 20px 0;
            line-height: 1.5;
        }

        .btn {
            text-decoration: none;
            background: white;
            color: #ff758c;
            padding: 10px 20px;
            font-size: 1rem;
            border-radius: 5px;
            font-weight: bold;
            transition: 0.3s;
        }

        .btn:hover {
            background: #ff758c;
            color: white;
        }

        /* Cat Animation */
        .cat {
            position: absolute;
            width: 60px;
            height: 60px;
            background: url('https://i.imgur.com/5SxnPGt.png') no-repeat center/contain;
            animation: float 15s infinite linear;
            opacity: 0.9;
        }

        .cat:nth-child(1) {
            left: 10%;
            animation-duration: 12s;
            animation-delay: 0s;
        }

        .cat:nth-child(2) {
            left: 25%;
            animation-duration: 14s;
            animation-delay: 3s;
        }

        .cat:nth-child(3) {
            left: 45%;
            animation-duration: 10s;
            animation-delay: 5s;
        }

        .cat:nth-child(4) {
            left: 65%;
            animation-duration: 16s;
            animation-delay: 2s;
        }

        .cat:nth-child(5) {
            left: 85%;
            animation-duration: 18s;
            animation-delay: 4s;
        }

        /* Floating Keyframes */
        @keyframes float {
            0% {
                top: 110%;
                transform: translateX(-10%) rotate(0deg);
            }
            50% {
                transform: translateX(10%) rotate(45deg);
            }
            100% {
                top: -10%;
                transform: translateX(-10%) rotate(360deg);
            }
        }
    </style>
</head>
<body>
    <!-- Floating Cats -->
    <div class="cat"></div>
    <div class="cat"></div>
    <div class="cat"></div>
    <div class="cat"></div>
    <div class="cat"></div>

    <!-- Main Content -->
    <div class="container">
        <h1>To My Love, Sherilyn ❤️</h1>
        <p>
            You are the light of my life, my inspiration, and my greatest treasure. <br>
            Every moment with you feels like magic, and I’m so lucky to have you by my side. <br>
            Sherilyn Valeriano, you mean the world to me. I love you more than words can ever say. ❤️
        </p>
        <a href="#" class="btn">Forever Yours 💕</a>
    </div>
</body>
</html>
